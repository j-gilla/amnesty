# decoders-niger-delta-task-presenter

To start dev build:

- Clone the repo and navigate to the directory

- 'yarn install'

- 'yarn start'

- Navigate to http://localhost:8080/ on the browser.

To build for production
- 'yarn build' => everything is output in ./dist folder
